# Cloudflare Worker secrets are configured with `wrangler secret put`.
# For local Worker preview, copy .dev.vars.example to .dev.vars.
# The project uses the ALYAM_DB Cloudflare D1 binding; no external database connection string is needed.
